# ---- AI (optional) -------------------------------------------------------
# 未設定でも全機能が動作します（ルールベース推論にフォールバック）。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# LLM_ENABLED=off  # LLMを完全に無効化したい場合

# ---- Backend -------------------------------------------------------------
DATABASE_URL=sqlite:///./ai_pmo.db
UPLOAD_DIR=./_uploads
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ---- Frontend ------------------------------------------------------------
NEXT_PUBLIC_API_BASE=http://localhost:8000
